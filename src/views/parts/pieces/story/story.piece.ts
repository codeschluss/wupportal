import { Component } from '@angular/core';
import { BasePiece } from '../base.piece';

@Component({
  selector: 'story-banner',
  styleUrls: ['story.piece.sass'],
  templateUrl: 'story.piece.html'
})

export class StoryPieceComponent extends BasePiece {

}
